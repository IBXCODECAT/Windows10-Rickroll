Set shell = CreateObject("WScript.Shell")

startupResponse = MsgBox("Would you like to remove the rickroll on startup?", 32 + 4)
tskmgrResponse = MsgBox("Would you like to remove the task manager rickroll?", 32 + 4)
Set shell = CreateObject("WScript.Shell")

startupResponse = MsgBox("Would you like to be rickrolled when your machine starts?", 32 + 4)
tskmgrResponse = MsgBox("Would you like to be rickrolled when attempting to open task manager?", 32 + 4)